import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'is-todo-mine',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
